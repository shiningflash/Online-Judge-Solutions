// @author :: Amirul Islam 
 
#include <bits/stdc++.h>
using namespace std;
#define LLI long long int
#define PII pair <int, int>

const int mx = 1e6;
const double pi = acos(-1.0);

int main() {
    // freopen("in", "r", stdin);
    
    double l, p, q;
    cin >> l >> p >> q;
    cout << (l / (p + q)) * p << "\n";
}
