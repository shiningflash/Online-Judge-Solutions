#include <bits/stdc++.h>
using namespace std;

int a[1000005];

int main() {
    int t, n, q, x, b, c;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> x;
    cout << (int) log2(n) + 1 << endl;
    return 0;
}
