#include <bits/stdc++.h>
using namespace std;
#define LLI long long int

int main() {
    // freopen("in.txt", "r", stdin);

    int a, b;
    cin >> a >> b;
    cout << __gcd(a, b) << endl;

    return 0;
}
